server_uri: bolt://localhost:7687
admin_user: neo4j
admin_pass: cuneyt123

mod: async
thread_count: 2

files:
  - url: /home/cnytync/Dev/dbpedia/long-abstracts_lang=fr_uris=en.ttl.bz2
    compression: bz2
    type: ttl
    chunk_size: 1000
    cql: |
      WITH $dict.rows as rows UNWIND rows as row
        MERGE(subject:Entity {dbpedia_uri: row.subject})
        SET subject.description=row.object
